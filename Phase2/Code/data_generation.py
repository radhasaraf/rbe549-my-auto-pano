#!/usr/bin/evn python
# load the image 
# select some perturbation limits
# discard some percent boundary for active region
# decide patch size M x N
# decide multiple perturbations per patch
# decide stride for patch selection on same image
# order in which we send data 
# is homography matrix always invertible? if not, what are the corner cases?
# location of the patch in the transformed image?